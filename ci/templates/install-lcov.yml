steps:
  - bash: |
      mkdir -p _deps && cd _deps
      curl -L http://ftp.de.debian.org/debian/pool/main/l/lcov/lcov_2.4.orig.tar.xz -o lcov.tar.xz
      tar -xf lcov.tar.xz
      mv lcov-2.4 lcov
    displayName: 'Install LCov'
